# Workload + Schema (read-only, compact)

## Schema (tables -> columns)
{schema_summary}

## Workload digest (JOIN/WHERE/GROUP/ORDER)
{workload_digest}

## Per-query summary (baseline vs candidate)
{query_summary}

# Current Program Information
- Metrics: {metrics}
- Fitness: {fitness_score} | Feature coords: {feature_coords} over [{feature_dimensions}]
- Improvement areas: {improvement_areas}
- Stats: {stats_summary}

# Current Program
```{language}
{current_program}
```

# Task
Suggest better index candidates by editing ONLY `INDEX_CANDIDATES` using SEARCH/REPLACE diffs.

### Output format (compact spec)
Each entry must be a dict with:
- `t`: table name
- `k`: key columns list in order (max 3; prefer composites when helpful)
- `m`: index method (optional; defaults to `btree`) e.g. `btree`, `gin`, `gist`, `brin`, `hash`, `spgist`
- `n`: short note explaining intent

Constraints:
- Max 20 candidates total.
- Do NOT change anything outside `INDEX_CANDIDATES`.
- Do NOT add code fences. Do NOT add commentary. Output ONLY the diff block.

Diff format (must match exactly):
<<<<<<< SEARCH
EXACT code to be replaced (copy-paste from Current Program)
=======
New code to insert
>>>>>>> REPLACE

Example:
<<<<<<< SEARCH
INDEX_CANDIDATES = []
=======
INDEX_CANDIDATES = [
    {{"t": "title", "k": ["production_year"], "n": "range filter on production_year", "m": "btree"}},
]
>>>>>>> REPLACE

{evolution_history}
